{
  "name": "@rts/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser game: top-down WebGL2 instanced sprite renderer + mobile touch UI over @rts/sim.",
  "scripts": {
    "dev": "node build.mjs serve",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@rts/sim": "*",
    "@rts/ai": "*",
    "preact": "^10.24.0",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
